name = "maheshbuilds"
compatibility_date = "2025-09-16"

# Serve the built Vite SPA (dist/) as static assets from a Worker.
# SPA routing: any unmatched path falls back to index.html.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
